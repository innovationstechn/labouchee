import 'package:flutter/material.dart';

const API_ADDRESS = 'http://laboucheesa.com/api';
const API_TIMEOUT = Duration(seconds: 10);
const API_KEY = '1234';

const DEFAULT_LOCALE = Locale('en');
final NAVIGATOR_KEY = GlobalKey<NavigatorState>();

